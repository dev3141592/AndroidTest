package test.test.random_user;

/**
 * Класс для наслідування, щоб розміщатися на сторінці
 */
class LoadedPageElement extends SimpleView {


    public LoadedPageElement(MainActivity act, int resId) {
        super(act, resId);
    }
}
